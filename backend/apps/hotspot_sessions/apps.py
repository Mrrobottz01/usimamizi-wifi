from django.apps import AppConfig


class HotspotSessionsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.hotspot_sessions'
    label = 'hotspot_sessions'
    verbose_name = 'Hotspot Sessions'
